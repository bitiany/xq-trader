---
name: sentiment-analysis
description: 监控个股新闻舆情、分析市场情绪倾向、捕捉事件驱动信号。当用户询问某股票近期新闻、舆情风向、重大事件影响、市场情绪时触发。
keywords: 舆情, 情绪, 新闻, 消息面, 事件驱动, 利好, 利空, 公告
---

# 舆情与情绪面分析

## 触发条件

用户询问某股票的近期新闻、舆情风向、重大事件影响、市场情绪时触发，例如：
- "XXX 最近有什么新闻"
- "XXX 最近的舆情怎么样"
- "XXX 有资产重组的消息吗"
- "市场情绪如何"

## 可用工具

来自 MCP 分组 `xq_stocks` 与 web 工具。

| 工具名 | 用途 | 关键参数 |
|--------|------|---------|
| `mcp_xq_stocks_xq_get_stock_news` | 个股新闻列表 | `symbol` |
| `mcp_xq_stocks_xq_get_stock_announcements` | 个股公告列表 | `symbol` |
| `mcp_xq_stocks_xq_get_stock_fund_flow` | 资金流向（验证情绪） | `symbol`, `limit` |
| `web_search` | 搜索市场情绪、行业政策、突发事件 | `query` |
| `web_fetch` | 抓取指定新闻全文 | `url` |

## 关键词体系

### 利好类
资产重组、回购、业绩预增、股权激励、大额订单、战略合作、增持、政策扶持

### 利空类
业绩预减、违规处罚、股东减持、商誉减值、退市风险、诉讼仲裁、监管问询

### 政策类
降准降息、产业政策、监管新规、财政刺激、贸易政策、行业整顿

## 执行流程

1. **获取新闻**：调用 `mcp_xq_stocks_xq_get_stock_news` 拉取个股近期新闻。
2. **获取公告**：调用 `mcp_xq_stocks_xq_get_stock_announcements` 拉取近期公告。
3. **补充搜索**：如新闻不足，用 `web_search` 搜索 "{股票名称} 最新消息" / "{股票名称} 重大事件"。
4. **情绪研判**：
   - 逐条标注新闻情感倾向（正面/负面/中性）与强度（1-5）
   - 识别重大事件（利好/利空/政策），分类归集
   - 汇总生成情绪指数（贪婪/中性/恐慌）
5. **交叉验证**：调用 `mcp_xq_stocks_xq_get_stock_fund_flow` 看资金流向是否与情绪一致（如利好但资金流出，需提示分歧）。
6. **输出报告**：新闻摘要 + 情绪研判 + 事件驱动信号 + 风险提示。

## 输出格式

```
# {股票名称}（{股票代码}）舆情与情绪面分析
> 数据截至：{trade_date}

## 一、新闻摘要
{近期重要新闻列表，每条标注来源与日期}

## 二、公告要点
{近期关键公告摘要}

## 三、情绪研判
- 情绪指数：{贪婪/中性/恐慌}
- 利好因素：{具体事件}
- 利空因素：{具体事件}
- 政策影响：{政策类事件}

## 四、事件驱动信号
- 信号类型：{买入/卖出/观望}
- 依据：{事件与资金流向综合判断}

## 五、风险提示
{需关注的潜在风险}
```

## 约束

- 新闻仅作辅助参考，不替代平台结构化数据。
- 情绪判断须基于具体新闻内容，不可凭空推断。
- 资金流向与情绪矛盾时必须提示分歧。
- 输出遵守 AGENTS.md Markdown 排版规范，段落间最多 1 空行。
- 不给出具体买卖建议，仅提供情绪面参考。

## 示例对话

用户: "比亚迪最近有什么新闻"
步骤:
1. 调用 `mcp_xq_stocks_xq_get_stock_news`（symbol="002594.SZ"）
2. 调用 `mcp_xq_stocks_xq_get_stock_announcements`（symbol="002594.SZ"）
3. 逐条标注情感倾向，汇总情绪指数
4. 调用 `mcp_xq_stocks_xq_get_stock_fund_flow`（symbol="002594.SZ"）交叉验证
5. 输出舆情报告

用户: "最近半导体行业有什么政策利好"
步骤:
1. 调用 `web_search`（query="半导体行业 最新政策利好"）
2. 整理政策类新闻，标注来源与日期
3. 筛选受益标的，输出政策影响分析
